{
  "name": "eatatl",
  "version": "1.0.0",
  "private": true,
  "description": "EatATL restaurant directory (static + Vercel serverless Places proxy)",
  "scripts": {
    "build": "node -e \"console.log('No build step (static site).')\"",
    "dev": "vercel dev",
    "start": "vercel dev",
    "lint": "node -e \"console.log('No lint configured')\""
  }
}

